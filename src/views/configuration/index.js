import React from 'react';
import {Text, View} from 'react-native';

export const ConfigurationView = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text style={{color: 'black'}}>Configuration Twitter</Text>
    </View>
  );
};
